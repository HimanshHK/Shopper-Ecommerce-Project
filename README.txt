To run the webapp just type "nodemon server.js" on termainal.
then go to localhost: 3000 to access the web app.


You have to add your own stripe key and local host port in a folder named .env
code example:
SERVER_URL=http://localhost:3000
STRIPE_PRIVATE_KEY="__yourkey___"